CREATE DATABASE stocks;

GRANT ALL PRIVILEGES ON DATABASE stocks TO julian;

CREATE TABLE stockdump
  (
  datetime timestamp,
  price float,
  stock_name text,
  created_at TIMESTAMP DEFAULT NOW()
  );
